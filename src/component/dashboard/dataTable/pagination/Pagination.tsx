import styled from "styled-components";

const PaginationWrapper = styled("div")``;
const Pagination = () => {
  return (
    <PaginationWrapper>
      <div>{"<<"}</div>
    </PaginationWrapper>
  );
};

export default Pagination;
